package write

import "github.com/mavolin/corgi/file"

type lib struct {
	lib *file.Library
}

func writeMixins(ctx *ctx) {
	// make(map[string]lib)
	//
	// for _, f := range ctx._stack {
	//
	// }
}
